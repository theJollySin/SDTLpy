language: python
python:
  - "3.8"
install:
  - pip install -r requirements.txt
  - pip install codecov
  - pip3 install pyflakes

script:
  - coverage run tests.py
  - python3 -m pyflakes
after_success:
  - codecov